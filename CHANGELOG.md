# Changelog

本博客（Astro + Vercel）的更新记录，按日期版本整理。

---

## v2026.08.25 — 思维导图系统（与文章深度联动）

### 核心功能
- **全新思维导图系统**（simple-mind-map 引擎）：
  - 管理后台：导图列表 + 编辑器（拖拽 / 折叠 / 快捷键 / 多主题 / 自动保存 / 导出 PNG·SVG·大纲）
  - 一键按文章标题结构**生成导图骨架**
  - 文章页入口 → 右侧抽屉查看（多图 tab），**点击引用节点跳转文章段落并高亮**
  - **选中文章片段加入导图引用**（弹框命名，非粘贴全文）
  - **拖拽到任意节点**：节点边框亮起提示 → 松手命名 → 插入该节点下
  - 写作页左右分栏：**边写文章边编辑导图**（＋引用选区）
  - 折叠/展开按钮常显、大小可调；编辑页支持绑定/改绑文章
- 后台导航新增入口（思维导图 / 设置）

### 关键修复
- 保存失败根因：本地 `DATABASE_URL` 被系统环境变量覆盖 → `.env` 优先
- 导图数据嵌套损坏根因：`setData` 误用完整格式 → 改用 `setFullData` + 自动修复存量数据
- 拖拽目标节点判定错误（坐标换算）→ 改用 `getBoundingClientRect`
- 文章页加载导图失败优雅降级（不再 500）

### 部署与稳定性
- PG 连接池上限降低 + 空闲回收（缓解 Prisma Postgres 连接数超限；建议把 `DATABASE_URL` 换成 `prisma_app` 应用连接串）
- PG 迁移 `0010`（mindmaps 表）

---

## v2026.08.24 — 评论 · 字体 · 外观

### 评论系统（自建，替换 Twikoo）
- 全新自建评论区：GitHub 登录 / 匿名双身份、嵌套回复、点赞（幂等取消）、最热/最新排序、默认前 6 条最热 + 更多、折叠/展开、编辑区折叠、作者/管理员删除
- 点赞按 GitHub / 匿名拆分计数（管理员看拆分明细，访客看总和）
- GitHub 登录后自动跳回来源页（OAuth state 携带 next，防开放重定向）

### 字体系统
- 设置页新增「外观」分区（字体 + 背景）
- 文章字体 / 其他字体分开设置（系统 / 内置 / 自定义导入），全站 @font-face 注入；内置思源宋体、站酷快乐体
- 手动字体设置优先于主题；未设置时跟随主题（可一键恢复）
- 本地导入字体（≤3MB）+ Vercel Blob 直传大字体（最高 30MB，绕过函数 4.5MB 限制）+ 子集化脚本

### 外观与体验
- 动态页毛玻璃有界容器（消除两侧空旷）
- 暗色模式文章正文强制浅色（可读性修复）
- 日历：本地时区「今日」、查看全部重要日期弹窗、日记悬浮预览
