---
/* ===== Imports ===== */
/*
 * 设置首页：直接复用 SettingsLayout，保证分区导航与子页面「同一份清单」。
 *
 * ⚠️ 这里曾经写死过一份只有 4 项（站点名称/诗词轮播/落地页/外观）的 nav，
 * 后续新增的分区（GitHub 图床 / Markdown 样式 / 全站样式 / AI 助手 / 网盘对接）
 * 都只加在了 SettingsLayout 里，导致从「后台 → 设置」进入时看不到新增分区。
 * 统一交给 SettingsLayout 渲染，避免再次出现两处清单不同步。
 */
import SettingsLayout from '@/layouts/SettingsLayout.astro';
---
<!-- ===== Template ===== -->
<SettingsLayout section="site-name" />
